t_color	ray_cast(t_pos const pos,
		t_camera const camera,
		t_object const * const objects,
		t_light const * const lights)
{
	t_ray		ray;
	t_object	nearest;
	double		distance;

	distance = -1;
	nearest = new_object
	ray = new_ray(env->camera, new_pos(x, y, 0), env->img.dim);
}
